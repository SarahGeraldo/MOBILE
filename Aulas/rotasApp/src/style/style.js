import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#0A25D7",
  },

  btn: {
    position: "absolute",
  },

  btnText: {
    fontSize: 60,
  },

  btnWall: {
    fontSize: 160,
  },
});
